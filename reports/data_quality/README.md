# reports/data_quality/

`latest.json` (machine-readable) and `latest.md` (human-readable) data
quality check results, regenerated every pipeline run by
`src/adip/validation/quality_report.py`. See "Data-quality methodology" in
the top-level README for what's checked and why.
